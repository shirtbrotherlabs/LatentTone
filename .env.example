# Host path to the music library (mounted read-only at /music).
MUSIC_LIBRARY=/mnt2/media/music

# Host path for persistent SQLite data
DATA_DIR=./data

# Browse UI published port
BROWSE_PORT=8080

# Canonical public / self-referenced origin for reverse-proxy deployments.
# Used by the server and SPA for absolute URLs (MediaSession artwork, links).
# Prefer HTTPS when exposed to Android / mobile (Wake Lock requires a secure context).
# Local-only: http://localhost:8080 is fine; default matches the proxied site.
PUBLIC_BASE_URL=https://latent.lt.lkeng.org/
# Alias also accepted by the Go server: LATENTTONE_PUBLIC_URL
